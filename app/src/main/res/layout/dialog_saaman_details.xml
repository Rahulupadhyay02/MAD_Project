<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_saaman_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:text="Saaman Name"/>

    <!-- Image container with fixed height -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/edit_text_background">

        <ImageView
            android:id="@+id/iv_saaman_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Saaman Image"/>
            
        <TextView
            android:id="@+id/tv_no_image_details"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No image available"
            android:layout_gravity="center"
            android:textColor="@color/gray"
            android:visibility="gone"/>
    </FrameLayout>

    <TextView
        android:id="@+id/tv_saaman_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:gravity="start"
        android:layout_marginBottom="16dp"
        android:text="Saaman Description"/>

    <Button
        android:id="@+id/btn_share"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Share This Item"
        android:drawableStart="@drawable/ic_share"
        android:paddingStart="8dp"/>
</LinearLayout> 